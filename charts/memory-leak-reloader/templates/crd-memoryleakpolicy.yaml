{{- if .Values.crds.install }}
{{- /*
  The MemoryLeakPolicy CRD is generated by `make manifests` into files/ (the
  single source consumed by both this chart and the envtest harness). Templating
  it here (rather than the chart's crds/ dir) lets `helm upgrade` update the schema.
*/}}
{{ .Files.Get "files/memreload.io_memoryleakpolicies.yaml" }}
{{- end }}
